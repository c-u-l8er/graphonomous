import Config

# Production environment configuration.
#
# Keep this file minimal and use `config/runtime.exs` for values that depend
# on runtime environment variables (secrets, endpoints, paths, etc.).

config :logger, level: :info

# Placeholder for production-specific overrides:
# config :graphonomous,
#   db_path: "/var/lib/graphonomous/graphonomous.db",
#   embedder_backend: :auto
