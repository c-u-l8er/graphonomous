# R7A — Source census for Graphonomous G0 dataset A: the ComputeDriven invariant program

Read-only census, 2026-09-02. Every count below was produced by a script over the named files or git refs (scripts and the machine-readable twin `R7A_census.json` sit beside this file). Nothing under `~/ProjectAmp2` or `~/.invariant-factory` was created, edited or deleted. Where something is absent it says NOT FOUND.

## 0 · Revisions the census is pinned to

| root | revision | notes |
|---|---|---|
| `~/ProjectAmp2/invariant-r10/` (own repo, no remote) | **census pinned to commit `699fbc2859efdaf57b2391d51dd7712b471e92b9`** (HEAD at start; branch `main`; tree `d668a7e89db2f5fdcf6797a029b7533443ecf674`; `git status --porcelain \| wc -l` = **0 at start**) | 440 tracked files at the pin. `699fbc2` is "R0.8.7 handback staged in px13 … unadjudicated"; the **v4 return identity** cited by `START_HERE_GPT.md` is commit `0270e811…` / tree `b48d04e7…`, two commits below. `invariant-r10.bundle` (git-ignored) heads at `1426428…`, one behind the pin. **HEAD moved during the census** (a parallel session committed its v2.7 work on top — see the next row and `R7A_census.json → roots.invariant-r10.{head_at_final_write, commits_since_pin}`); every count and hash below is taken from the pin, and `package-v2.6/` is tree-identical between the pin and the new HEAD. |
| live-tree caveat | porcelain grew from 0 to **24 lines during the census** (final list in `R7A_census.json → roots.invariant-r10.porcelain_at_end`) | First `?? package-v2.7-staging/` (15:54:47 local; the census began 15:43:15), then `?? package-v2.7/`, `?? inputs-gpt-execution-adjudication-v4.md` (the GPT v4 adjudication arriving), `?? tools-lane/{build,drift_gate}_v2_7.py`, and ` M` edits to `experiments/s5_effect_adapter/effect_identity_model.py`, both Super/TRVM handoffs and the four `tools-lane/factory-kit/` files. None of it was written by any command here — a parallel session was authoring the v2.7 patch, and before this census finished it **committed** that work (porcelain back to 0, HEAD past the pin). **Every sha256 in this census for a tracked invariant-r10 file is the blob at `699fbc2`** (`git show 699fbc2:<path>`), so the numbers are pinned regardless of the drift; where the working tree differed at the JSON's final write it also carries `worktree_sha256`. `package-v2.6/` itself is tree-identical at the pin and at the new HEAD. |
| `~/.invariant-factory/canonical.git` | `refs/heads/invariant-canonical` = **`d217ee29a3322c68db0d43be47491f0e9d4fbc64`**, tree **`11ab2c6192ecf19fbf974da7b6483899903eca32`** = tag `INV-R9.4`; HEAD symref → that branch; 15 commits on the ref; 111 tree paths | `log -3`: `d217ee29` INV-R9.4 (2026-08-26 16:51), `0cc11141` INV-R9.3, `887637be` INV-R9.2, all by "invariant-factory station". Other heads: `invariant-quarantine` (`8aab4de0` = tag INV-R6.2-MIXED), `invariant-unpromoted-r71` (`9eb24ecf`). 19 tags INV-R6 … INV-R9.4. |
| `~/.invariant-factory/wt-r9` | detached HEAD `2913613e…` = tag INV-R8.3; **5 commits behind** the ref (ancestor: yes); 56 porcelain lines | Verified: of the 20 modified + 40 untracked-in-worktree files, **60 are byte-identical to the ref's blobs** and only 3 review-bundle products (`INVARIANT_CURRENT_BRIEF.md`, `INVARIANT_REVIEW_BUNDLE.md`, `invariant-review.zip`) are not in the ref. Its `CLAIM_LEDGER.json` sha256 `9ff6ac3f…` equals the ref's. The label is stale, not the content; **the ref is authoritative**. |
| `~/ProjectAmp2/opensentience.org/` (sub-repo) | HEAD `2c0f523dc5975a0c8939f59cfe7a7be638448073`, `main`, tree `458debc9…`, clean, origin `git@github.com:c-u-l8er/opensentience.org.git` | `_invariants/data/cells.json` blob `5d3dd702…` is **byte-identical** to the factory ref's copy; `axes.json` (`3b028ec8…` vs factory `d0e58335…`) and `copy.json` (`c471ee4f…` vs `1c514230…`) differ by wording only. |
| `~/ProjectAmp2/computedriven/` | HEAD `21a1452eb6de183292e398872a4e83176566790b`, `master`, clean | Commits `efa8881`, `747f025`, `21a1452` all present; the R10 lane vendored `efa8881`. |
| `~/ProjectAmp2/CLAIM_LEDGER.json` (+ `mosaic/`) | tracked by ProjectAmp2 git; `_round` **INV-R7.8** (2026-08-25), **190 claims**, sha256 `b5a94117…` | Not named in the brief but load-bearing: the site build (`_invariants/build/build.mjs`, `ROOT = resolve(SITE,'..')`) reads *this* ledger, and the served page's `dist/artifact.json` says `LEDGER_CLAIM_COUNT 182 @ INV-R7.6`. This is the R7 lane — the content source of the factory's `INV-R9-from-INV-R7.5` integration — not the canonical ref. `wt-r83` holds a fourth state (INV-R8.3, 151 claims). |

Authority classes used below: **AUTHORITATIVE** (owns its records) · **DERIVED** (computed/projected/built from other registries) · **HISTORICAL** (superseded versions, old rounds, stale checkouts) · **ADVISORY** (GPT/Fable adjudication prose, briefs, handoffs, unminted drafts).

## 1 · A1 — `invariant-r10/package-v2.6/` (candidate final canonical)

### 1.1 Inventory

41 files, 612 KB. Current-facing: `README.md`, `12_R10PRE_SYNTHESIS.md`, `INV_FRONTIER_R10_PRE.md`, `11_R10PRE_OPUS_HANDOFF.md`, `06_R10PRE_EXPERIMENTS.md`, `CROSS_REGISTRY_CLAIM_MAP.json`, `evidence_state.json`, `10_MACHINE_READABLE_LEDGER.json`; `witnesses/` (3 `.py` + 3 `.output.txt` + `.tla` + `.cfg`); `tools/drift_gate.py` (+ `.output.txt`); `inputs/` (5 GPT rulings); `v1-provenance/` (13 files); three countersign/ingestion notes; `EXTERNAL_RESEARCH_PINS_V3.md`. sha256/blob per file: `R7A_census.json → sources[]` and `hashes.json`.

### 1.2 `CROSS_REGISTRY_CLAIM_MAP.json` — AUTHORITATIVE (for E-records; self-declared `status: "DRAFT — preserves registry origin; nothing merged into CLAIM_LEDGER.json…"`)

sha256 `910cf6ea…`, blob `4639a28d…`, 85,064 B, `crosswalk_version: "r10-pre-v2.6-2026-09-02-v3-adjudication-applied"`, 22 top-level keys.

| facet | derived value |
|---|---|
| records | **56**; ids `^E-\d{2}[abc]?$` (56/56 match; no duplicates; numbers 1–51 with no gap; `E-13a/b/c`, `E-46a/b/c`, `E-50a/b` are splits — `split_from` ∈ {E-13:3, E-46:3, E-50:2}) |
| `source_registry` | r10pre-execution 13 · cd-core-docs 11 · trvm-law-registry 8 · cd-durable-docs 7 · ampd-readme 5 · wrl-core 5 · wek-w-laws 4 · claim-ledger 3 |
| `semantic_obligation` | S2 11 · S3 10 · S4 9 · S1 7 · S5 7 · CROSS 6 · DEF 3 · REPR 3 (declared vocabulary also lists `COR`; **used on no record**; `S6?` on no record) |
| `relation` | corollary 27 · mechanism 8 · representation 7 · direct 6 · cross-cutting 5 · definition 3 |
| `evidence_class_token` | TESTED 20 · PROPERTY-TESTED 8 · in_tree 6 · FROZEN 5 · SPEC 4 · FALSIFIED-KEPT-RED 4 · live_local 2 · TESTED-CONDITIONAL 2 · PROVED 2 · EXHAUSTIVE-IN-MODEL 2 · ARGUMENT 1 (11 tokens; `evidence_class` free text has 27 distinct values, several paragraph-long) |
| `executed:true` | 8 records (E-13b, E-14, E-15, E-48, E-49, E-50a, E-50b, E-51); `executed_by` "this lane" 5 / "computedriven lane" 2; `runtime` strings on the same 8 |
| `promotions[]` | 5 (E-13b, E-14 in v2.6; E-15, E-48 in v2.5; E-51 in v2.6); `sensitivity_witness.type` pre-fix-fail 4 · mutation-verdict-flip 1; every `receipt` + `sha256` (9 incl. pre-fix/repair) **recomputed MATCH** |
| other blocks | `resolved_candidates` {S6?} · `liveness_candidates` {L1?: 11 keys, 3 witnesses} · `factory_candidates` {FAC-CONTROL-SENSITIVITY: 9 keys, `generic_rules_v3` A–H, 7 witness families} · `r0_8` (7 keys, 5 open findings, 8 closures) · `untested_locus_roles` 10 · `not_found_artifacts` 4 · `ontology` 5 levels · `witnesses` 5 sha256 (all MATCH the files) · `execution_summary_v2_3/v3/v4` · `adjudication_v2_4/v2_5/v2_6` |

Cross-reference fields and resolution (tested against the package dir, `invariant-r10/`, the computedriven working tree **and** `computedriven@efa8881`, `super/`, `TRVM/`, `WRL/`):

| field | points at | total | resolved | dangling |
|---|---|---|---|---|
| `source_ids` (77, 66 distinct) | registry-local ids in one string field: computedriven doc/receipt/source paths (`docs/admission-model.md`, `cd-core/src/locus.rs LifecycleAdmission`), TRVM law ids (`law:world.write-mediated@1`), `WRL_CORE_0.2 §7`, WEK `W-1`, `ampd/test effect (7)`, `claim-ledger:EMB-AUTH-NONAMP`, `cells.json:27a`, experiment labels `EXP-6 C2` | 33 path-like / 44 non-path | 28 path-like resolve (26 in the computedriven tree, `cd-core/src/locus.rs` and `authority.rs` also at `efa8881`) | 5 path-like dangle, all labels rather than paths: `EXP-3/W3`, `EXP-3/W4`, `cells.json:16 ⊥`, `cells.json:27a`, `RB-DEV/RB-ANCHOR/RB-WITNESS` |
| `witness_paths` (93, 82 distinct; 16 records empty: E-10, E-25–E-38, E-41) | files, often with `#anchor` or a prose suffix | 93 | **92** (58 via `invariant-r10/`, 17 computedriven, 10 package, 4 `super/`, 2 ProjectAmp2, 1 TRVM) | **1**: `E-15 → EVIDENCE-nc17-19-23-AFTER-efa8881.txt` (NOT FOUND anywhere; `px13/nc17-18-19-23-live-tamper.txt` is the likely intended file) |
| `derivation_links` (61; 19 records have none) | mixed: 24 crosswalk ids (`E-02`, `E-01`, `E-14`, `E-13` — note `E-13` no longer exists as a record after the split, 4 links still name it), 15 obligation prose, 2 law ids, 1 factory claim id, 19 free prose ("lesson: …", "Kleppmann fencing token…") | 61 | 20 resolve to a live record id | 4 name the retired `E-13`; the rest are prose, not references |
| adjudication refs | free text inside `evidence_class`/`name`/`derivation_links`: 58 `§` mentions, 33 distinct (`GPT v3 §12`, `GPT v2 §2`, `§21.2` …) | — | — | no structured field; no file pointer |

Commit hashes appear only inside prose (16 distinct: `efa8881`, `747f025`, `af1cbc2`, `7651697`, `cfde255`, `4e2dc54` …); **0 line-number references** anywhere in the file.

### 1.3 `evidence_state.json` — DERIVED (`schema: "evidence-state@1"`, `source: "CROSS_REGISTRY_CLAIM_MAP.json r10-pre-v2.6-…"`, built by `tools-lane/build_v2_6.py`)

sha256 `1edb46d3…`, blob `8737c78c…`. 56 records (ids identical to and in the crosswalk's order), 5 promotions (with `history[]`), 1 status (`R0.8: OPEN`, 5 open findings), 3 artifacts (`witnesses/liveness_l1.py` executed:true, `liveness_l1_fair.py` executed:true, `L1Succession.tla` executed:**false**), `derived_counts {records:56, promotions:5}` (equal to the actual lengths). `class` == crosswalk `evidence_class_token` for all 56; `class_prev` == `evidence_class_token_v2_5` for all 56 (`null` for the 3 records new in v2.6: E-50a, E-50b, E-51). `executed`: true 8 / null 48. `conditions`: list on 3 (E-13b, E-14, E-15), null on 53. `trust_profile`: non-empty string on 42. `receipts`: 80 bare strings over 40 records (0 receipts on 16); **79 resolve, 1 dangles** (`E-15 → EVIDENCE-nc17-19-23-AFTER-efa8881.txt`, the same dangling `witness_path`) — with two caveats on the "resolved" side: the four `E-17/19/20/21 "ampd/test effect (7)"`-style entries resolve only because `super/ampd/test` is a directory (they are test-suite labels, not files), and `E-22 governance/probe_worldalias_v02_repro.mjs` resolves under `TRVM/` (24,320 B, 2026-08-18), i.e. against a live tree the package never pinned. By root: invariant-r10 45 · computedriven 17 · package 10 · super 4 · ProjectAmp2 2 · TRVM 1. In 53/56 records `receipts` equals `witness_paths`; E-48 narrows to the two `run_exp6.log` files. Validators re-run here without writing anything. `python3 tools/drift_gate.py .` → **`DRIFT GATE PASS` (12 files, 56 records, 5 promotions)**; on `../package-v2.5` → `DRIFT GATE FAIL (99)`. `check-evidence-transitions.mjs evidence_state.json` → **✓ ADMISSIBLE (56 records · 5 promotions)** when run at 15:5x against the committed receipts; `--selftest` → every one of 11 mutations refused. **Re-run at 16:0x (validator blob taken from the pin) it is REFUSED: `B E-51: receipt sha256 mismatch for experiments/s5_effect_adapter/effect_identity_model.output.txt`** — the parallel session rewrote that receipt (mtime 16:05:04) and then committed it; the blob at `699fbc2` still equals the recorded `6966c054…`, the blob at the new HEAD does not. All nine promotion receipts MATCH their recorded sha256 at the pin (`R7A_census.json → sources[A1.evidence_state].xref_fields…sha256_check` carries the pin-vs-worktree verdict per receipt). The lesson is a census finding in its own right: a receipt cited by relative path is pinned only inside a commit, and `package-v2.6` names no commit for its receipts.

### 1.4 `10_MACHINE_READABLE_LEDGER.json` — AUTHORITATIVE for the research-lane claims; its `source_registries_derived` block is DERIVED

sha256 `262d2ed0…`, blob `b1cdc96b…`. `research_version: "fable51-invariant-discovery-2026-09-02-v2.6-v3-adjudication-applied"`. **19 claims**: `P1 P2 P3a P3b P4a P4b P5 C-1…C-7 X-1…X-4 D-1` (regex `^(P\d[ab]?|C-\d|X-\d|D-\d)$`). Status used: TESTED 6 · EXHAUSTIVE-IN-MODEL 4 · PROPERTY-TESTED 2 · SPEC 2 · REFUTED-AS-STATED 1 · REDUNDANT 1 · REJECT 1 · UNKNOWN 1 · DEFINITION 1 — all inside the 15-token `status_vocabulary` (PROVEN, EXHAUSTIVE-IN-MODEL, TESTED, PROPERTY-TESTED, SUPPORTED, SPEC, PLAUSIBLE, SPECULATIVE, REFUTED, REFUTED-AS-STATED, REDUNDANT, REJECT, OUT-OF-SCOPE, UNKNOWN, DEFINITION). `semantic_map` (14): `P1→S1, P2→S2, P3a/P3b→S3-mechanism, C-1→S3-witness, P4a/P4b/C-3→S4, P5/C-4→S5, C-7→S6?, C-2→Locus-narrowed, C-5→reworded, C-6→CROSS`. Versus `v1-provenance/10_MACHINE_READABLE_LEDGER_v2_as_delivered.json` (same 19 ids) exactly two statuses moved: **P5 SUPPORTED→TESTED, C-7 UNKNOWN→TESTED**. `source_registries_derived` records what the lane consumed: `CLAIM_LEDGER.json {round INV-R9.4, claims 208, by_status REFUTED 19 / PROVED 52 / CONDITIONAL 3 / KNOWN 7 / OPEN 46 / DECLARED 71 / MEASURED 3 / PROVISIONAL 7}`, `cells.json {version 0.8, cells 46}`, `invariant-grid.json {1.47.0, 94 entries}`, `computedriven_git_bundle {head efa8881, commits 62}`, `not_found [WEK_R0_REPO_MAP.md, world-to-world communication spec, spatial navigation spec, live repository access, R0.8.3 lab receipt]` — the factory numbers agree with §2 below.

### 1.5 The remaining A1 sources

| source | class | derived facts |
|---|---|---|
| `INV_FRONTIER_R10_PRE.md` (sha `7daec2be…`), `12_R10PRE_SYNTHESIS.md` (`18e5f1d2…`), `06_R10PRE_EXPERIMENTS.md` (`897e81bd…`), `11_R10PRE_OPUS_HANDOFF.md` (`c44a1b4e…`), `README.md` (`8fa86cc7…`) | DERIVED prose (gate-scanned) | frontier §4 = the S6? rule and outcome, §7 = L1?, §8 = FAC rule; synthesis §1 table is the S1–S5 map |
| `witnesses/adversarial_worlds.py` `439ed40b…` / `liveness_l1.py` `2e3804d1…` / `liveness_l1_fair.py` `0d48e3e5…` (+ `.output.txt`) | AUTHORITATIVE executable witnesses | outputs end `19 expectations, 0 failed` / `6 expectations, 0 failed` / `19 expectations, 0 failed`; all five sha256 in `crosswalk.witnesses` MATCH the files |
| `witnesses/L1Succession.tla` `abc19edb…` + `.cfg` `c8864761…` | unexecuted artifact (`artifacts[].executed:false`) | not a receipt; GPT v3 §11 "not a blocker" |
| `tools/drift_gate.py` `52739920…` (= `tools-lane/drift_gate_v2_6.py`) + `drift_gate.output.txt` | DERIVED validator | rules A–H + L1 assertions; 13 `--mutate` names; root-level `invariant-r10/drift_gate.py` is the **v2.2** gate (byte-identical to `package-v2.2/tools/drift_gate.py`) |
| `inputs/` — `FABLE51_RESEARCH_GPT56_ADJUDICATION_FOR_OPUS.md`, `FABLE51_R10PRE_V2_GPT56_AUDIT_FOR_OPUS.md`, `FABLE51_R10PRE_V21_GPT56_FINAL_AUDIT_FOR_OPUS.md`, `R10PRE_EXECUTION_V2_GPT56_ADJUDICATION.md` (= root `inputs-gpt-execution-adjudication-v2.md`), `R10PRE_EXECUTION_V3_GPT56_ADJUDICATION.md` (= root `…-v3.md`) | **ADVISORY** | 5 files, though README says "the four GPT-5.6 rulings"; the **v1** execution adjudication (`inputs-gpt-execution-adjudication.md`, sha `34e3afc8…`, whose §1 is the S6 ruling) and the GO ruling exist only at the repo root |
| `v1-provenance/` (13 files) | HISTORICAL | "pre-adjudication analysis … not current guidance", not gate-scanned |
| `experiments/*/RESULT.md` — `authority_merge` (EXP-5), `distributed_currentness` (EXP-1), `locus_capability_facade` (EXP-4), `micro_locus_s3` (EXP-8), `observe_is_write` (EXP-3), `s5_effect_adapter` (EXP-6 + EXP-6b addendum), `s6_locus_birth` (S6? falsifier), `trvm_contracts` (EXP-7 + v4 addendum) — 8 files; plus `experiments/README.md`, `rust/results/*.md` (EXP-4/EXP-5-cd-core/S6 outputs), 201 tracked files, 520 MB with vendored builds | AUTHORITATIVE receipt narratives | EXP-2 is NOT FOUND as a directory (the numbering skips it; EXP-8 lives in `micro_locus_s3`) |
| `handoffs/` — `FAC_CONTROL_SENSITIVITY_CANDIDATE.md` (HEAD sha `ed6ebaf4…`), `SUPER_E50_EFFECT_IDENTITY.md` (`0cec6602…`), `TRVM_E42_E43_CANDIDATE_LAWS.md` (`3f51ff05…`) | ADVISORY (to the factory / Super / TRVM lanes) | nothing registered by them; the Super and TRVM handoffs were edited in the working tree by the parallel session after 15:58 local (worktree hashes `f80b42f4…`, `9b7f8f25…` — recorded in the JSON, not used here) |
| `px13/` — 24 files, 18 MB, all tracked | AUTHORITATIVE copies of the computedriven lane's receipts | R0.8.3 extracts (`series.txt` 21/21, `R0.8-20260901T035400Z.final-receipt.json`, nc files) + handbacks whole: R083 tar (1.7 MB), R084 md+tar (2.4 MB, `0b87256`), R085 (3.9 MB, `af1cbc2`), R086 (4.8 MB, `747f025`, F35), **R087 (5.4 MB, `21a1452`, F36/F37 — staged, unadjudicated, in no package)**. `SHIPPED-SHA256SUMS.txt` (23 entries) **verifies**. `README.md` head: "PX13 — R0.8.3 lab: EXECUTED (2026-09-01); R0.8 remains OPEN; … R0.8.7 staged (F36/F37), unadjudicated". |
| `CHANGELOG.md` `42feddea…`, `START_HERE_GPT.md` `eb11d8fd…` | DERIVED narrative | START_HERE's identity table: v2.2 `5e2e5d44`/`776f3f49`, v2.3 `808c522e`, v2.4 `f5861402`, v2.5 `6c15f641`, v2.6 `0270e811`/`b48d04e7` |
| `SHA256SUMS` (git-ignored, 441 entries) | DERIVED manifest, **stale** | `sha256sum -c`: `px13/README.md` FAILED, `px13/SHIPPED-SHA256SUMS.txt` FAILED; the two R087 files are absent from it |
| `tools-lane/factory-kit/` | DERIVED validator + ADVISORY draft | `FAC-CONTROL-SENSITIVITY.claim.json` (status DECLARED, obligation universal_affirmation, evidence_kind "mutation", 6 witnesses at factory-relative paths that do not exist yet) — **NOT FOUND in the canonical CLAIM_LEDGER.json** |
| `package-v2.2/…/v2.5/`, `*-staging/` | HISTORICAL | 32 / 31 / 33 / 39 files; v2.5 was ACCEPTED-with-patch by GPT v3 and v2.6 supersedes it |

## 2 · A2 — the invariant factory canonical ref

### 2.1 `CLAIM_LEDGER.json` @ `invariant-canonical` — AUTHORITATIVE

Blob `23141cd1…`, sha256 `9ff6ac3f…`, 458,585 B. Top-level: `_comment`, `_statuses` (8), `_settled` (3), `_round` `{id: INV-R9.4, date: 2026-08-26, receipt_ref: mosaic/receipts/INV-R9.4.json}`, `claims`, `_revisions` (derived domain: `pre_receipt` [INV-R1, R2, R2.1, R3, R3.1, R4.1] + every `mosaic/receipts/INV-*.json`), `_registries` (11 keys; gates = `check-mosaic.mjs`, `check-mutation-surface.mjs`, `check-lineage.mjs`, `factory-station.mjs`).

| facet | derived value |
|---|---|
| claims | **208**, no duplicate `claim_id`; regex `^[A-Z0-9]+(-[A-Z0-9.]+)+$` (208/208); prefixes FAC 40 · FED 38 · MOS 29 · EMB 22 · ASR 19 · LED 17 · IMPACT 9 · TAX 6 · EVID 5 · ESC 4 · INC 3 · LINEAGE 3 · SUPPORT 3 · MEDIATION 3 · REPRO 2 · ADMISSION 2 · TERM/CONTROL/RECEIPT 1 each |
| `_statuses` (with definitions) | PROVED · CONDITIONAL · REFUTED · KNOWN · MEASURED · DECLARED · PROVISIONAL · OPEN; `_settled.statuses` = [PROVED, CONDITIONAL, REFUTED, KNOWN, MEASURED] |
| status used | DECLARED 71 · PROVED 52 · OPEN 46 · REFUTED 19 · KNOWN 7 · PROVISIONAL 7 · CONDITIONAL 3 · MEASURED 3 (none outside `_statuses`; **84 settled**) |
| `evidence_kind` (86 claims) | deductive_proof 26 · counterexample 17 · constructive_witness 16 · exhaustion 15 · cited_result 8 · measurement 4 |
| `obligation` (85) | universal_affirmation 38 · existence 22 · universal_refutation 19 · universal_absence 4 · rate_bound 1 · bounded_absence 1 |
| `refutation_scope` (19) | timeless 14 · INV-R2.1 2 · INV-R7.3 2 · INV-R1 1 |
| `last_verified` | 2026-08-23 15 · 08-24 126 · 08-25 1 · 08-26 66 |
| fields | required 9 on all; `assumption_refs` 140 · `review_focus` 96 · `evidence_kind` 86 · `obligation` 85 · `gap` 72 · `imported_from` 43 · `readjudicated` 43 · `evidence_qualifiers` 29 · `refutation_scope` 19 · `supersedes` 19 · `superseded_by` 13 · `finding` 3 |
| `witnesses` | 269 refs over 171 claims (37 claims have none: OPEN 21, KNOWN 6, PROVISIONAL 6, REFUTED 2, DECLARED 2); 137 carry a `§SECTION`; **269/269 resolve** in the ref's tree (scripts 209 · mosaic 50 · AmpersandBoxDesign 8 · opensentience.org 2) |
| `implementation_binding` | `cell:NN` on **54** claims → 7 distinct cells (`01, 24, 25, 27a, 27b, 36, 45`; cell:36 alone takes 27), all resolve to `cells.json`; path bindings on 122 (all in tree); null on 32 |
| provenance fields | `imported_from {source_revision, source_status, artifact}` 43 · `readjudicated {by, on, method, authority_transferred}` 43 · `supersedes` prose 19 · `evidence_qualifiers {citation_ref SRC-*, applicability, mapping_argument_ref ARG-*}` 29 |

Vocabulary of the A1 world in this ledger: mentions of `E-\d\d` **0**, `R0.8` **0**, `computedriven` **0**, `invariant-r10` **0**, `crosswalk` **0**, `locus` **0** (the two `S1..S5` hits are unrelated). The whole canonical tree (JSON + MD) mentions `FAC-CONTROL-SENSITIVITY` **0** times. The two registries are disjoint; the crosswalk's `claim-ledger` records (E-39, E-40, E-41 → `EMB-AUTH-NONAMP`, `EMB-CUT-EMPTY`, `TAX-RELATIONAL-2/TAX-FLOW`) are the only bridge, and it points one way.

### 2.2 `mosaic/` — AUTHORITATIVE registries; `derived/` DERIVED; `receipts/` AUTHORITATIVE transition receipts

| file | version/round | record lists (count) | id scheme |
|---|---|---|---|
| `evidence.json` | v4 / INV-R6 | `instruments` 12; vocabularies `kinds` 11, `qualifiers` 12, `obligations` 9, `consumption` 11, `status_obligation_domain` 8, `origins` 4 | `INS-*` |
| `assumptions.json` | v2 / INV-R7.5 | `assumptions` 29 (`kinds` 7, `discharge_states` 6) | `ASM-*` |
| `arguments.json` | v4 / INV-R9 | `arguments` 27 (`roles` 7) | `ARG-*` |
| `sources.json` | v2 / INV-R7.5 | `sources` 24 (one per cited RESULT) | `SRC-<AUTHOR>-<YEAR>-<TAG>` |
| `objectives.json` | v3 / INV-R6 | `soft_objectives` 5, `evaluators` 4 (`EVAL-FABLE-5`, `EVAL-OPUS-5`, `EVAL-GPT-56`, `EVAL-HUMAN`), `hard_constraints` H1–H7 | `SO-*`, `EVAL-*` |
| `occupancy.json` | v2 / INV-R6 | `rules` 3; `kind_vocabulary.declared` (6 tokens, the source cells.json reads) | `OCC-*` |
| `operations.json` | v1 / INV-R7.1 | `matrix` 6 operations × 8 quantities; status DECLARED | operation names |
| `embodiment.json` | v1 / INV-R6 | `capabilities` 4 (all synthetic), `battery`, `impact_algebra` | `CAP-SYNTHETIC-*` |
| `factory.json` | v1 / INV-R9.1 | `canonical_ref`, `quarantine_ref`, `git_dir ~/.invariant-factory/canonical.git`, `revisions` 21, `derivations` 12, `artifact_additions` 15, `working_tree_divergence` | revision ids `INV-R*` |
| `defeaters.json` | v2 / INV-R9.1 | `defeaters` 68, `incidents` 46 | `DEF-*`, `INC-R<n>-*` |
| `derived/` | — | `brief-facts.json` (18 facts, INV-R9.4), `measurements.json` (4 `MEAS-*`), `occupancy.json`, `integration-delta.json` | — |
| `integration/INV-R9-from-INV-R7.5.json` | INV-R9 | two-diff reviewed content delta; "R7.5 is a CONTENT SOURCE and never a git parent" | — |
| `receipts/` | — | **20** files: INV-R5, R5.1, R5.2, R6, R6.1, R6.2, R7.1–R7.5, R8-RECOVERY, R8.1-CONCURRENCY-OBSERVED, R8.2, R8.3, R9, R9.1–R9.4 | `INV-R<n>[.<m>][-TAG].json` |

Example receipt `INV-R9.4.json` (receipt_version 4, 18 top-level keys): `parent {revision INV-R9.3, commit_oid 0cc11141…, receipt_sha256, tree_paths 108}`, `candidate {revision, commit_oid "A COMMIT CANNOT CONTAIN ITS OWN OID. Recorded as the git tag INV-R9.4.", tree_manifest_sha256 1fda60f4…}`, `transition`, `tree_delta {created, modified, deleted}`, `source_artifacts`, `contracts`, `invariants {before 205, after 208, established [FAC-INTEGRATION-PORTABLE-REPLAY, FAC-INTEGRATION-NOT-STATED-NONPASSING, FAC-INTEGRATION-TRANSFORM-FIELD-CLOSED], retyped [FAC-NOT-STATED-NONPASSING PROVED→DECLARED], broken []}`, `authority`, `objectives`, `evidence {gates_run, audit_reproduction…}`, `scope`, `provenance {toolchain, digests}`, `decision {verdict promote, expected_parent_oid 0cc11141…, cas "One transaction over the canonical ref and the revision id INV-R9.4, or neither."}`, `lineage`, `honesty`, `next`.

### 2.3 Validation rules (from the script headers)

`scripts/check-claim-ledger.mjs` refuses: a status outside `_statuses`; a witness naming a missing file or a `§SECTION` the file lacks; PROVED/CONDITIONAL with no witness; CONDITIONAL with empty `assumptions`; `implementation_binding: cell:NN` absent from `cells.json` or contradicting the table status; duplicate/missing fields; a `.mjs` witness whose relative-import closure does not resolve; REFUTED without a `refutation_scope` in the derived revision domain; an UPPER bound cited for a LOWER-bound conclusion. `scripts/check-mosaic.mjs` (§1–§8): typed refs (`{kind: witness|claim|source}`) must resolve; consumption is checked against each claim's declared `obligation` through a *relevant* typed argument (role, `conclusion_claim`, `obligation_discharged`, cited source); measurements are computed and objectives may not present un-instrumented numbers; occupancy rules run over the table and derived views are regenerated and compared; negative tests reproduce the R5.1 exploits; the head receipt's copied counts are derived (§6) as are counted phrases in ledger prose (§7); open defeaters are the residual doubt (§8). `evidence.json.status_obligation_domain` constrains which obligations/kinds a status may carry (e.g. PROVED forbids `model_judgment`, `randomized_search`, `statistical_certificate`; KNOWN requires `cited_result`). `ROUND9_4_BRIEF.md` head: "I verified a portability claim in the one environment where portability was unnecessary" — parent INV-R9.3, 2026-08-26; §1 the reproduction command failed under `env -i`, §2 `FAC-NOT-STATED-NONPASSING` retyped to DECLARED, §3 a forwarded record's target-local fields fixed by vocabulary.

## 3 · A3 — `opensentience.org/_invariants/data/`

`cells.json` (sha256 `ea36e684…`, blob `5d3dd702…`, v0.8) — AUTHORITATIVE: **46 cells**, `num` is a *string* `^\d{2}[ab]?$` (01–45 with 27a/27b; file order is not numeric). Fields on all 46: `num symbol label status title tagline desc extra protocol protocol_group authority source`; partial: `kind` 13, `kind_source` 13, `tier` 12, `economic` 7, `formal` 7, `kind_why` 6, `proof` 6, `glyph` 4, `hypothesis` 3, `relational_arity` 2, `witnesses`/`witness_note`/`observer_model` 1. Authored `status` (six tokens defined in `axes.json.statuses`): missing 17 · sketched 9 · proved 8 · shipped 5 · named 4 · conditional 3. **Binding is reverse-only**: cells carry no claim ids (0 claim-id-like tokens in the file); `CLAIM_LEDGER.json` claims point at cells through `implementation_binding: "cell:NN"` (54 claims → 7 cells; 39 cells unbound), and `build.mjs` joins them, refusing a dangling `cell:NN` (R18). Claim-status × cell-status pairs on the bound claims: REFUTED→conditional 12, PROVED→conditional 12, OPEN→conditional 10, PROVED→proved 3, DECLARED→sketched 3, CONDITIONAL→conditional 2, REFUTED→proved 2 … (the register axis, not the cell status, is what the page derives). `axes.json` (`5bed9223…`): `registers` [decided, built, named, proposed] with mechanical rules (decided = `tier` set OR a settled ledger claim binds the cell), `statuses` (6), `tiers`, `kind_vocabulary_source → mosaic/occupancy.json`. `copy.json` (`ceb2495c…`): page copy with `{{PLACEHOLDER}}` counts. `dist/artifact.json` — DERIVED: round **INV-R7.6**, facts `CELL_COUNT 46, DECIDED 13, BUILT 4, NAMED 22, PROPOSED 7, BOUND_CLAIM_COUNT 54, BOUND_CELL_COUNT 7, REFUTED_CLAIM_COUNT 14, LEDGER_CLAIM_COUNT 182`, page sha256 `40af287e…` = the served `invariants.html`. The factory ref's copy of `invariants.html` (blob `645f3ce2…`) is a different build.

## 4 · Internal inconsistencies found

1. `11_R10PRE_OPUS_HANDOFF.md` line 18 still says the crosswalk has **51 records**; README line 13 and frontier §6 say 56, the JSON has 56.
2. `12_R10PRE_SYNTHESIS.md` §1 and §7 say `liveness_l1_fair.py` is **15/15**; the v2.6 witness prints `19 expectations, 0 failed`, frontier §7 and README say 19.
3. README says `inputs/` holds "the four GPT-5.6 rulings"; there are five files, and the v1 execution adjudication (the S6 ruling) is only at the repo root.
4. Frontier §5 numbering runs 1,2,3,4,5,6,**8,7**.
5. Root `SHA256SUMS` is stale (2 mismatches, 2 R087 files missing); it is git-ignored, so the committed tree does not carry the defect.
6. `invariant-r10.bundle` is one commit behind HEAD.
7. Root-level `drift_gate.py` is the v2.2 gate.
8. `derivation_links` still name `E-13` (4 times), a record retired by the E-13a/b/c split; `semantic_obligations` declares `COR`, used nowhere.
9. `evidence_state.records[].receipts` are bare strings — no per-receipt `executed`, sha256, or kind — so factory rule D is only checkable on the 5 promotions.
10. The served page reports 182 ledger claims @ INV-R7.6 while the canonical ledger is 208 @ INV-R9.4; the R10 ledger's `source_registries_derived` used the canonical 208.
11. `FAC-CONTROL-SENSITIVITY` exists as a draft claim in `invariant-r10/tools-lane/factory-kit/` and NOT in the canonical `CLAIM_LEDGER.json`.
12. `wt-r9`'s HEAD label (INV-R8.3) is stale while its content equals INV-R9.4.
13. The crosswalk `source_ids` field mixes paths, law ids, section references and free labels; `cells.json:16 ⊥`, `cells.json:27a` point at cells by number in a format no script resolves.
14. One `witness_path` dangles: `E-15 → EVIDENCE-nc17-19-23-AFTER-efa8881.txt`.
15. Promotion receipts are pinned by *relative path + sha256* into a shared working tree: during this census the E-51 receipt (`experiments/s5_effect_adapter/effect_identity_model.output.txt`) was rewritten and then committed by a parallel session, so `package-v2.6/evidence_state.json` is ADMISSIBLE at commit `699fbc2` and REFUSED (rule B) at the commit now on top of it — while `package-v2.6/` itself is byte-identical in both. The v2.6 package has no field that names the commit its receipts live in.

## 5 · The seven G0 acceptance questions — exact records

**q1 · "Why isn't S6 a primitive?"**
- Ledger claim `C-7` (`10_MACHINE_READABLE_LEDGER.json`): `"status": "TESTED"`, `"status_before_v2_3": "UNKNOWN"`, `"classification_v2_4": "S1 corollary (domain = world locus-birth / currentness); not an obligation"`, `"r10pre_execution": "S6? falsifier executed on frozen cd-core: two births of one LocusId under one root both act; a warden minted by one birth finalizes the other. Remedy (World admission) has S1's exact shape → RECORDED AS S1(domain = world locus-birth); not promoted as S6."`; `semantic_map["C-7"] = "S6?"` (the map still carries the retired label as the *origin*).
- Crosswalk `resolved_candidates["S6?"]`: `"world/locus birth uniqueness — TESTED 2026-09-02 (E-44) and RESOLVED as S1(domain = world locus-birth / currentness); not an obligation; no record carries S6? as a value"`. Record `E-44` (`semantic_obligation: "S1"`, `relation: "corollary"`, `source_registry: "r10pre-execution"`, `source_ids: ["S6? falsifier"]`, `evidence_class: "TESTED (frozen cd-core API, vendored @ efa8881; scratch crate x-capfacade)"`, `derivation_links: ["S6? RESOLVED: S1(domain = world locus-birth / currentness) — not a distinct obligation"]`, witnesses `experiments/rust/results/S6_locus_birth.md`, `experiments/rust/src/bin/s6_birth.rs`, `experiments/rust/compile-fail/cf_s6_world_admission_reuse.rs`, `cf_s6_root_is_private.rs`). `adjudication_v2_4.applied[0] = "S1–S5 current map; S6? retired → resolved_candidates"`.
- Experiment: `experiments/s6_locus_birth/RESULT.md` — "Disposition: S6? → recorded as a scoped corollary of S1 (domain = world locus-birth). Crosswalk E-44; ledger C-7 UNKNOWN → TESTED."
- Adjudication: `invariant-r10/inputs-gpt-execution-adjudication.md` §1 "S6? ruling — ACCEPT the reduction to S1" (ll. 116–176): "I do not see a residual semantic property that requires a distinct S6." — with the promotion rule stated earlier in `package-v2.6/inputs/FABLE51_R10PRE_V2_GPT56_AUDIT_FOR_OPUS.md` §6: "Promote S6 only if a counterexample demonstrates a property not expressible as: S1(domain = world locus-birth/currentness)". Prose surface: `INV_FRONTIER_R10_PRE.md` §4.

**q2 · "Why is R0.8 still open?"**
- Crosswalk `r0_8`: `"status": "OPEN"`, `"why": "F35 successor provenance is open for adjudication; power-loss and post-rename durability are unclaimed; single host only (GPT v3 §8)"`, `"f35_status": "OPEN-UNADJUDICATED (handback shipped whole this return)"`, `open_findings[5]` (F35 successor provenance; power-loss/fsync UNCLAIMED; post-rename/directory-fsync OPEN design question; single-host only; trusted deployment anchor / filesystem namespace as an explicit trust-profile ASSUMPTION), `closed_by_adjudication_v3[8]` (F31, F23/F24, F32, F33, F34, F24, F22, F30), `historical[3]`, `shipped_not_adjudicated[1]` (R0.8.6 @ 747f025). `r0_8_status` repeats it in one string. `E-13b.conditions[2]`: "successor provenance is NOT covered by this closure (F35 …) until R0.8.6 is adjudicated".
- `evidence_state.json.statuses[0]`: `{"id": "R0.8", "status": "OPEN", "open_findings": [5 strings]}` — rule E requires this list to be non-empty.
- Receipts: `px13/COMPUTEDRIVEN_R086_FOR_GPT.md` ("F35 REPRODUCED, THEN REPAIRED … R0.8 yours to rule on") and `.tar.gz` (sha `cffc0218…`); staged beyond any package: `px13/COMPUTEDRIVEN_R087_FOR_GPT.{md,tar.gz}` (F36 "the candidate root taught the trusted anchor what to authorize", F37 anchor reservation window).
- Adjudication: `inputs/R10PRE_EXECUTION_V3_GPT56_ADJUDICATION.md` §2 "F35 is OPEN FOR GPT ADJUDICATION", §8 "R0.8 remains OPEN", §18 item 2, §21.2.
- Outside the pin (for orientation only): the v4 adjudication committed at the new HEAD rules "F35 EXACT ATTACK CLOSED … R0.8 REMAINS OPEN FOR ONE NEW CRASH-CUT CONTROL", so at v2.7 the answer to q2 shifts from F35 to the R0.8.7 residue (F36/F37, `px13/COMPUTEDRIVEN_R087_FOR_GPT.*`). Not censused here.

**q3 · "What currently supports S5?"**
- Records with `semantic_obligation == "S5"`: E-16 (ARGUMENT), E-17, E-18 (in_tree, ampd), **E-48** (mechanism, `evidence_class_token: "TESTED"`, `executed: true`, `executed_by: "this lane"`, `runtime: "Elixir 1.19.4 (Erlang/OTP 28) · git 2.55.0 · Linux 7.2.2-arch1-1 · 8 BEAM boots, ~6 s"`, `tested[6]`/`not_tested[5]`, `receipts: ["experiments/s5_effect_adapter/receipts/run_exp6.log", ".../receipts-run1-key-only-reconcile/run_exp6.log"]`, 12 `witness_paths` incl. the `phase_*.json` receipts; promotion `sensitivity_witness {type: pre-fix-fail, receipt: experiments/s5_effect_adapter/receipts/phase_crash_naive_after.json, sha256 21569669…}` MATCH), **E-50a** and **E-50b** (`FALSIFIED-KEPT-RED`, `split_from: "E-50"`, `scope_profile: "ampd @ super 7651697; any far side; the Super lane's to repair"`), **E-51** (`EXHAUSTIVE-IN-MODEL`, `receipts: ["experiments/s5_effect_adapter/effect_identity_model.output.txt"]`, sha `6966c054…` — MATCH at the pinned commit `699fbc2`, no longer matching the copy the parallel session rewrote and committed afterwards, `runtime: "python3 3.13.14"`). Ledger: `P5` `"status": "TESTED"` (was SUPPORTED), `C-4` `"r10pre_execution": "v2.5: + TESTED live (EXP-6) …"`. Prose: `experiments/s5_effect_adapter/RESULT.md`, frontier §1 S5 row, `handoffs/SUPER_E50_EFFECT_IDENTITY.md`; adjudication v3 §12 ("Move S5 to TESTED under the exact stated profile"), §13.

**q4 · "What assumptions/scopes constrain this receipt?"**
- `E-13b` carries `conditions[3]` ("the deployment anchor namespace is trusted, canonical, and not attacker-selectable…", "single host; one LocusId → one canonical incarnation lock + one durable lineage record", "successor provenance is NOT covered…"), `trust_profile: "single host · one protected deployment-owned canonical deployment anchor namespace · deployment-controlled root/anchor parent directory · one LocusId → one incarnation lock · anchor pins revision + head; cross-machine uniqueness not claimed; F35 open"`, `scope_profile`, `sensitivity_witness`, `pre_fix_witness` (R084 tar, NC24 FAIL kept), `repair_witness` (R085 tar), `evidence_class_v2_4`/`v2_5` history.
- `E-14` carries `conditions[2]`, `committed[3]`, `not_claimed[3]` ("adversarial replacement of the SQLite engine binary itself", "arbitrary hostile custom collation/function implementations…", "availability against a hostile store (DoS)"), `trust_profile: "fixed / trusted SQLite interpreter; intended schema contract verified; canonical ordering in Rust; defensive configuration"`, `scope_profile`.
- The rule: factory-kit rule C (a class containing CONDITIONAL needs non-empty `conditions` AND a `trust_profile`); in `evidence_state.json` only 3 records have `conditions` lists, 42 have a `trust_profile` string, so scope is a *string* on most records and a *list* on the conditional ones.

**q5 · "Which mechanism implements this obligation?"**
- There are **no MECHANISM records** in any of the three registries. Mechanisms are represented four ways: (a) `relation: "mechanism"` on 8 E-records — E-13a, E-13b, E-13c, E-14, E-15 (S3, `source_registry: cd-durable-docs`, `source_ids` like `docs/durable-authority-model.md R0.8.2`, `receipts/R0.8.3-FALSIFIED.md F1`, `tools/store_read_gate.sh`), E-36 (S3, WEK `W-6`, `W-14`, `RB-DEV/RB-ANCHOR/RB-WITNESS`), E-48 (S5, `EXP-6`), E-49 (S4, `EXP-7`); (b) code symbols inside `source_ids` strings on exactly two records — `E-01: "cd-core/src/locus.rs LifecycleAdmission"`, `E-02: "cd-core/src/authority.rs reconstruct"` (both files exist at `computedriven@efa8881` and in the working tree; the symbol after the space is prose); (c) the frontier's "ENFORCEMENT MECHANISM (by substrate profile)" column and the five-level `ontology` where MECHANISM INSTANCE is a level, not a record type; (d) `liveness_candidates.L1?.mechanism_instances_seen: ["cd-core FinalityWarden + LossBasis …"]`. The factory mosaic has instruments (`INS-*`), arguments, assumptions and an operations matrix, but no mechanism type either.

**q6 · "Which claims are unsupported by an executed receipt?" — first cut from `evidence_state.json`**
- 22 records have a TESTED* class (20 TESTED + 2 TESTED-CONDITIONAL). Of these: `executed: true` on **5** (E-13b, E-14, E-15, E-48, E-49), `null` on 17; **21** have ≥1 receipt string, **E-10** has none; **0** receipts carry an `executed: true` marker, because receipts are bare strings — `artifacts[]` marks only the three L1 files. Decidable from data: the 5 promoted/executed TESTED* records (plus E-50a/b, E-51 outside TESTED*) are receipt-backed with runtime identity and matching sha256. Undecidable from data, decidable only by opening the files: the other 17. A proxy by file kind shows 10/22 TESTED* records cite an execution output or bundle (`.json/.txt/.log/.tar.gz`), while **11 cite only code or prose**: E-01, E-02, E-03, E-04, E-05, E-06, E-07, E-08, E-09 (source_ids/witnesses like `tools/compile-fail/*.rs`, `receipts/R0.7.md`), E-11, and E-44 (`S6_locus_birth.md` + `.rs` sources — its `rust/results/summary.json.txt` exists but is not cited). E-10 (TESTED, `docs/failure-model.md §Identity vs validity`) has no witness at all. Everything above the TESTED* line — 8 PROPERTY-TESTED, 5 FROZEN, 4 SPEC, 6 in_tree, 2 PROVED, 1 ARGUMENT — has no `executed` flag and, for 16 of them, no receipt.

**q7 · "Where did this relation come from?"**
- Present per E-record: `source_registry`, `source_ids`, `witness_paths`, `scope_profile`, `derivation_links`, `split_from`, the version trail `evidence_class_before_v2_3 / _v2_4 / _v2_5 / evidence_class_token_v2_5`, `promoted_in`, `executed/executed_by/runtime`, `adjudication_v2`/`adjudication_v3` (2 records), `r10pre_execution_note`; adjudication provenance is prose (`"GPT v3 §12"`, 58 mentions); commit identity is prose (`efa8881`, `747f025`, `7651697`, `fd0df4c`).
- Missing per E-record: a commit OID per source, line numbers or byte ranges (0 in the file), a per-record timestamp (only the package date), a structured `{file, section}` pointer to the adjudication, a sha256 on `witness_paths` entries (only promotion receipts and the 5 package witnesses are hashed), and a stable id for the *relation* itself (a `derivation_link` is an unkeyed string).
- On the factory side the equivalent machinery exists and is typed: `imported_from {source_revision, source_status, artifact}`, `readjudicated {by, on, method, authority_transferred}`, `supersedes`/`superseded_by`, `last_verified`, `refutation_scope` over a derived revision domain, `evidence_qualifiers {citation_ref → SRC-*, mapping_argument_ref → ARG-*}`, `assumption_refs → ASM-*`, and a transition receipt binding `parent.commit_oid` + a tree-manifest sha256 — but no per-claim commit OID and `witnesses` cite `path §SECTION`, never a line.

## 6 · What the dataset builder should take from this

The E-records (56), the 19 research-lane claims, the 208 factory claims and the 46 cells are four disjoint id spaces with exactly three typed bridges: `10_MACHINE_READABLE_LEDGER.json.semantic_map` (P/C → S), `CLAIM_LEDGER.json.implementation_binding` (claim → `cell:NN`), and the crosswalk's `source_registry: claim-ledger` / `source_ids: claim-ledger:<id>` (3 records). Everything else that reads like a relation — `derivation_links`, `evidence_class` prose, `§` citations, commit hashes — is free text that a G0 ingester will have to parse, and the census numbers above say how much of it resolves today (witness paths 92/93, evidence receipts 79/80, factory witnesses 269/269, cell bindings 54/54, path-like `source_ids` 28/33). One more caution for the ingester: the tree is live. During this census a parallel session built `package-v2.7/`, dropped the GPT **v4** adjudication into `invariant-r10/` (`inputs-gpt-execution-adjudication-v4.md`, sha `eaf80161…`, disposition line: "SCIENTIFIC RESULTS GO. F35 EXACT ATTACK CLOSED. `package-v2.6` STRUCTURE ACCEPTED, BUT A v2.7 EVIDENCE-STATE PATCH IS REQUIRED BY THIS ADJUDICATION. R0.8 REMAINS OPEN FOR ONE NEW CRASH-CUT CONTROL."), and at 16:07:27 committed it all as the **v5 return** (`303f76c` "v5 return: package-v2.7 per the GPT-5.6 v4 execution adjudication …", then `ba4e625` START_HERE v5). So `package-v2.6` — "candidate final canonical" at the pin — was superseded by `package-v2.7` before this census finished. Whatever G0 ingests should be taken from a commit, never from the working tree; this census is of `699fbc2`, and a v2.7 delta census is the obvious next step.
