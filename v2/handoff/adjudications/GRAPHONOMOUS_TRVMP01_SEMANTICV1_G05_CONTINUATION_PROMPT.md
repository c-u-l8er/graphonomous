# Continuation prompt — TRVM-P0.1 → Graphonomous semantic.v1 → G0.5 read-only demo

You are Fable 5.1 working in the real ProjectAmp2 tree.

Read:

1. `graphonomous/v2/handoff/STATUS.md`
2. `graphonomous/v2/handoff/DECISION_LOG.md` through D-059
3. `graphonomous/v2/handoff/G0D_GOLDEN_VECTORS.md`
4. `graphonomous/v2/handoff/G0F_V1_OBLIGATION.md`
5. `graphonomous/v2/handoff/G0F_EVIDENCE_PROFILE_NOTE.md`
6. `graphonomous/v2/handoff/STACK_FIX_RECEIPTS/TRVM-P0.md`
7. `GRAPHONOMOUS_G0D_G0F_GPT_ADJUDICATION_v5.md`

Record GPT v5 rulings as new decisions/amendment pointers. Never silently modify a frozen profile/protocol.

## Mission

Complete one visible milestone:

**close TRVM-P0.1 → build the smallest honest `graphonomous.semantic.v1` from measured G0-F omissions → seal/certify it → build the first minimal read-only Graphonomous UI over the proven stack.**

Stop for GPT review after G0.5.

Do not begin broad G1, autonomous truth mutation, evidence.v0, or the TRVM primitive-basis round.

---

# Phase A — record the accepted G0-D/G0-F goldens

Record:

- current G0-D baseline/historical/multi certificate vectors at TRVM `9e91c96`;
- multi v0 projection root, snapshot commitment and `sem-b8d828…`;
- `GRAPHONOMOUS-PROJECTION-v0` semantic meaning as frozen;
- re-mint terminology: historical code-bound certificates "verify under their pinned verifier coordinates";
- v0 remains frozen;
- evidence.v0 remains NOT YET;
- semantic.v1 is OPEN.

Do not rewrite historical certificate ids.

---

# Phase B — TRVM-P0.1 normative closure

This is an owning-TRVM spec/release closure, separate from Graphonomous.

## B1. Dedicated refusal codes

Revise the normative nested-composition refusal vocabulary so malformed supplied child-protocol entries and attempted built-in overrides have distinct codes.

Choose stable names consistent with the existing TRVM vocabulary after inspecting the normative schema.

Requirements:

- do not weaken/remove `spec_agreement`;
- update the normative schema/release artifacts through the repository's normal procedure;
- old 28 refusal meanings remain intact;
- existing built-in child protocol vectors remain byte/identity stable unless the normative release identity is explicitly designed to move;
- new tests assert code, not only detail;
- no application-specific `graphonomous` branch.

## B2. Blind-run ledger

Record the already-performed blind-run supersession in the authoritative TRVM owning ledger if such a ledger exists/permits this update:

superseded:
`brun-c39b708f1d96f2b6df1562d66c33c3eee3d13e2307274e42adbff79025b13ad8`

aborted:
`brun-740403c7…`

current pinned:
`brun-74f54466247c3ccbfcf15ada42242605c7a299961a8b7413e1c914f18fe8c264`

Do not invent a ledger if TRVM has no authoritative place for it; in that case document precisely why the existing receipt is canonical enough.

## B3. Verification-policy rule

Add a conformance/documentation assertion that a supplied child checker's `VERIFIED` is meaningful only together with the verifier-policy/checker-set coordinates.

No consumer test may equate:

`VERIFIED@policy-A`

with:

`VERIFIED@policy-B`

merely because the verdict token is the same.

## B4. Receipt

Run the relevant TRVM batteries and create a TRVM-P0.1 receipt/local commit.

Do not touch derivation `prim`s.

---

# Phase C — `graphonomous.semantic.v1` pre-freeze audit

The second source has already justified opening v1. Do not wait for a third source.

Start from `G0F_V1_OBLIGATION.md`, but treat its 2-role / 1-kind / 9-pair surface as a **candidate**, not frozen truth.

## C1. Census every refused authoritative factory structure

At factory pin `d217ee2`, enumerate and classify at least:

- all 27 ARGUMENTs;
- all 68 DEFEATERs;
- all defeater target forms, especially 34 `consumption_rule` targets;
- all argument target dictionaries / source coordinates;
- incident FINDINGs;
- assumption discharge refs;
- instrument records;
- objective/evaluator/hard-constraint/process registries.

For every omitted structure choose exactly one:

- REPRESENTED in v1;
- DEFERRED with a concrete consumer-based reason;
- SOURCE-REPAIR required;
- OUT-OF-SCOPE process metadata.

Preserve source bytes/locations for every deferred item.

## C2. Role/kind rulings already accepted

Use these unless the census finds a direct contradiction:

- ARGUMENT is a distinct role;
- DEFEATER is a distinct role;
- DISCHARGED_BY is a distinct relation kind.

Do not retype ARGUMENT as CLAIM.
Do not retype DEFEATER as FALSIFIER.

## C3. Do not guess consumption-rule semantics

Do not add `CONSUMPTION_RULE`, misuse `SOURCE_LOCATION`, or stretch `MECHANISM` just to eliminate omissions.

Inspect the real target records and determine what identity the source actually supplies.

If the source gives only code coordinates, the honest v1 may preserve the target as source-addressed evidence while deferring a semantic ATTACKS endpoint.

If it gives a stable rule identity, propose the smallest role/relation needed.

Record alternatives.

## C4. Incident findings

Measure whether factory incidents and existing FINDING semantics can share the FINDING role without accidental co-reference or semantic ambiguity.

Do not merge by textual similarity.

If `finding_source` is merely provenance, keep it out of semantic identity unless a query demonstrates otherwise.

## C5. Freeze proposal

Only after the audit produce `graphonomous.semantic.v1` declaration.

Requirements:

- v0 row untouched;
- v1 = smallest additive semantic contract justified by measured authoritative data;
- explicit delta table v0 → v1;
- every added role/kind/endpoint pair cites at least one pinned real source record;
- every still-omitted authoritative structure has a recorded disposition.

Stop for a ruling before implementation only if the audit exposes a genuinely ambiguous semantic choice with materially different identity consequences that cannot be reduced by source evidence. Otherwise choose the smallest source-grounded design and proceed.

---

# Phase D — implement v1 + factory argument/defeater layer

## D1. WRL owning layer

Add `graphonomous.semantic.v1` as a new static profile row through the already-accepted WRL-P0 mechanism.

Never edit v0.

Run WRL conformance and prove all v0 goldens unchanged.

## D2. Adapter

Extend the factory adapter additively for the v1-approved structures.

All v0 emitted records remain unchanged.

Do not parse prose/source forms that the audit classifies SOURCE-REPAIR.

## D3. Rebuild

Produce a v1 multi-source projection/evaluation while preserving all v0 projections.

Important:

The **Graphonomous projection** may become richer because new authoritative factory records are ingested.

The v0 world remains the frozen v0 semantic view of its frozen dataset/vector; do not rewrite the old golden.

Make snapshot/profile distinctions explicit.

## D4. Seal

Seal the v1 semantic world through WRL.

Produce:

- new v1 `sem-`;
- kernel-minted `rel-` / `rev-`;
- v0/v1 statement-LID correspondence;
- delta counts;
- endpoint/refusal vectors.

## D5. Certificate

G0-D certify the v1 projection.

If TRVM-P0.1 changed live verifier coordinates, re-mint current certificates under the new checker while preserving the old TRVM-P0 vectors as historical receipts.

Use the accepted terminology:
old receipts verify under their pinned verifier coordinates; the new checker may refuse them on coordinate mismatch without invalidating the old reconstruction.

---

# Phase E — G0.5 minimal read-only Graphonomous UI

The UI is now allowed because the semantic/evidence backend has passed two-source, WRL-seal and certificate tests.

Do not turn this into a full product redesign.

## Required surfaces

### 1. Snapshot/world selector

At minimum:

- baseline v0;
- historical v0;
- multi v0;
- multi v1.

Display clearly:

- snapshot id / source pins;
- projection root;
- G0-E evaluation root;
- G0-D certificate;
- WRL profile;
- WRL `sem-`.

Never mix records from two selected snapshot roots.

### 2. Semantic graph

Render statement nodes/relations from the WRL-sealed semantic world.

Use stable Graphonomous statement LIDs as cross-world names.

UI/layout coordinates must remain outside projection/WRL semantic identities.

### 3. Node inspector

Show:

- LID;
- role;
- semantic attrs;
- evidence-state vocabulary/token where present;
- source-family assertions;
- source locations;
- current snapshot.

For ARGUMENT/DEFEATER in v1, show their role-specific relationships without pretending deferred target semantics exist.

### 4. Relation inspector

Show separately:

- statement LID;
- relation kind/endpoints;
- WRL `rev-`;
- world-scoped WRL `rel-`;
- assertion occurrences;
- source locations.

Make the statement/revision/allocation distinction visible.

### 5. Explain panel

Use the existing G0-E `explain` output.

Clearly label:

- observed facts;
- derived facts;
- derivation depth/rule;
- `trvm_derivation:false`;
- exact source assertion leaves.

No LLM-generated explanation may replace the deterministic explanation tree in the acceptance demo. An LLM summary may be layered later and labelled advisory.

### 6. Fault panel

Display typed ingestion faults by code/source/snapshot and allow navigation to the affected record/location.

### 7. A1–A7 demo queries

Make the existing acceptance questions accessible from the UI:

- why S6? is not primitive;
- what opens R0.8 by snapshot;
- what supports S5;
- E-13b witness movement;
- mechanism evidence;
- receipt-observability partition;
- source-grounded explanation.

### 8. Certificate/identity panel

Make the four distinct coordinate systems visible:

- projection root;
- evaluation root;
- G0-D certificate/verifier coordinate;
- WRL semantic world `sem-`.

For historical certificates say:
"verifies under pinned verifier coordinates"
rather than merely "valid".

## UI authority boundary

G0.5 is READ ONLY.

It may not:

- promote/reject evidence;
- modify authoritative registries;
- mint claims;
- rewrite factory/crosswalk state;
- trigger stack repair autonomously;
- label an advisory LLM answer as deterministic derivation.

No write controls disguised as disabled future buttons in the acceptance screenshot/demo unless clearly labelled nonfunctional.

## Framework

Use the existing Graphonomous application direction and the lightest host UI that integrates cleanly with the current repository.

The UI itself need not be WRL-native.

The important demo claim is:

> the displayed semantic graph is derived from authoritative sources, deterministically projected, certified, and sealed as a real WRL world.

Avoid framework churn.

---

# Phase F — acceptance / handoff

Return `GRAPHONOMOUS_SEMANTICV1_G05_FOR_GPT.md` + ZIP + SHA256.

Include:

## TRVM-P0.1
- old/new TRVM commits;
- normative code additions;
- spec/release identity effects;
- blind-run ledger disposition;
- batteries/non-regression;
- receipt.

## v1 audit
- complete refused-layer census;
- every item's disposition;
- final v0→v1 delta;
- evidence for every new semantic primitive;
- unresolved source-repair items.

## v1 implementation
- WRL v1 profile row;
- v0 non-regression;
- v1 projection/evaluation roots;
- v1 G0-D certificate;
- v1 sem/rel/rev vectors;
- argument/defeater query examples.

## G0.5
- runnable instructions;
- screenshots if the environment can generate them;
- exact demo script;
- identity/source panel outputs;
- A1–A7 UI acceptance results;
- confirmation that UI/layout state does not move semantic identities.

## Evidence-profile checkpoint
Reassess `graphonomous.evidence.v0` only from concrete G0.5/v1 needs. Recommendation only; do not implement unless a new hard requirement makes the existing separation insufficient.

Stop for GPT adjudication after G0.5.
