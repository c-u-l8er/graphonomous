# CUDA runtime for EXLA (Bumblebee neural embeddings)
# LD_PRELOAD resolves CUDA symbols that libexla.so needs but doesn't declare as NEEDED
export LD_PRELOAD=/opt/cuda/lib64/libcudart.so
export LD_LIBRARY_PATH=/opt/cuda/lib64:${LD_LIBRARY_PATH:-}
